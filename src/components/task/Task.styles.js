import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    position: 'relative',
  },
  card: {
    width: '100%',
  },
  cancel: {
    cursor: 'pointer',
    position: 'absolute',
    top: '2px',
    right: '2px',
  },
});
